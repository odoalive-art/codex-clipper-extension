# Project

## Overview
Briefly describe the project purpose and intended users.

## Getting Started
1. Install dependencies.
2. Configure environment variables if needed.
3. Run the project locally.

## Common Commands
- `command for development`
- `npm run test:regression`
- `command for build`

## Repository Structure
- `docs/`: collaboration and project documentation.
- `src/` or equivalent: application source code.
- `tests/` or equivalent: test suites.

## Collaboration
- Start each session by reading `docs/ai-context.md` and `docs/todo.md`.
- Follow `AGENTS.md` and `PROJECT_RULES.md` when making changes.
