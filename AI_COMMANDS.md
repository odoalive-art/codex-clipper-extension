# AI Commands

## Purpose
该文件定义可复用的 AI 指令模板。

当用户输入：`执行【命令名称】`
AI 应匹配本文件中的同名命令，并按对应模板执行。

## Invocation Format
- 标准格式：`执行【命令名称】`
- 可选参数：`执行【命令名称】（参数：...）`

## Execution Rules
1. 命令名称必须与“命令库”中的名称完全匹配。
2. 若命令不存在，AI 需提示“未找到命令”并列出可用命令。
3. 若命令存在，AI 按模板顺序执行，不跳步。
4. 执行后输出：
   - 已完成内容
   - 修改文件列表
   - 下一步建议（如有）

## Command Template Format
每个命令使用以下结构：

```md
### 命令：<命令名称>

触发词：执行【<命令名称>】
目标：<命令目标>
输入：<可选输入>
步骤：
1. ...
2. ...
3. ...
输出：
- ...
```

## Command Library

### 命令：上下文同步

触发词：执行【上下文同步】
目标：在新会话/新设备快速恢复项目上下文。
输入：无
步骤：
1. 阅读 `AGENTS.md`、`PROJECT_RULES.md`。
2. 阅读 `docs/ai-context.md`、`docs/architecture.md`、`docs/todo.md`、`docs/dev-log.md`。
3. 扫描仓库主要代码文件，确认当前实现状态。
4. 输出当前目标、已完成事项、下一步优先任务。
输出：
- 上下文摘要
- 风险与阻塞项
- 建议优先级清单

### 命令：开始开发

触发词：执行【开始开发】
目标：按当前文档上下文启动一个开发任务。
输入：任务说明（可选）
步骤：
1. 从 `docs/todo.md` 选择最高优先级未完成任务（或使用用户指定任务）。
2. 明确任务范围与不修改范围。
3. 实施最小可验证改动。
4. 运行必要验证。
5. 更新相关文档（如状态变化）。
输出：
- 实施摘要
- 验证结果
- 修改文件列表

### 命令：结束会话

触发词：执行【结束会话】
目标：形成可交接的会话收尾记录。
输入：无
步骤：
1. 更新 `docs/ai-context.md` 的当前状态与下一步。
2. 更新 `docs/todo.md` 的任务状态。
3. 在 `docs/dev-log.md` 追加一条本次会话记录。
4. 汇总本次修改文件与后续建议。
输出：
- 会话总结
- 已更新文档
- 下一会话启动建议

### 命令：文档刷新

触发词：执行【文档刷新】
目标：根据当前代码状态校正文档一致性。
输入：无
步骤：
1. 读取主要代码与配置文件。
2. 检查 `docs/ai-context.md`、`docs/architecture.md`、`docs/todo.md` 是否过期。
3. 更新过期内容并保持模板结构清晰。
4. 在 `docs/dev-log.md` 记录本次文档更新。
输出：
- 文档变更摘要
- 修改文件列表

### 命令：代码审查

触发词：执行【代码审查】
目标：对当前改动进行风险导向审查。
输入：目标文件或范围（可选）
步骤：
1. 读取改动文件与关联逻辑。
2. 识别 bug、回归风险、边界条件与缺失测试。
3. 按严重级别输出发现项（高到低）。
4. 给出最小修复建议。
输出：
- Findings 列表（含文件位置）
- 风险总结
- 修复建议

### 命令：发布前检查

触发词：执行【发布前检查】
目标：在提交/发布前完成基础质量检查。
输入：无
步骤：
1. 检查工作区状态与变更范围。
2. 执行项目可用的测试/校验命令。
3. 复核文档是否与代码一致。
4. 输出发布风险与阻断项。
输出：
- 检查结果
- 阻断项（如有）
- 可发布结论（是/否）

### 命令：Git工作流

触发词：执行【Git工作流】
目标：按仓库约定执行多设备可恢复的分支开发流程。
输入：任务名（可选，未提供则由 AI 根据当前任务生成短任务名）
步骤：
1. 阅读 `docs/git-workflow.md` 并确认当前阶段（开工/开发中/切设备/收尾）。
2. 生成或确认分支名：`feature/<short-task-name>` 或 `fix/<short-task-name>`。
3. 输出该阶段应执行的最小命令序列。
4. 若涉及会话收尾，提醒同步更新 `docs/ai-context.md`、`docs/todo.md`、`docs/dev-log.md`。
输出：
- 当前阶段命令清单
- 推荐分支名
- 需要更新的文档列表

## Maintenance
- 新增命令时，使用“Command Template Format”统一结构。
- 命令名称应短、明确、无歧义。
- 命令步骤应可执行、可验证、可交接。
